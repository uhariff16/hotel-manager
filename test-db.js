import { createClient } from 'npm:@supabase/supabase-js'; import dotenv from 'npm:dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function check() { const { data, error } = await supabase.from('saas_payments').update({ razorpay_subscription_id: null }).eq('tenant_id', '00000000-0000-0000-0000-000000000000'); console.log('Update to null:', error ? error.message : 'Success'); } check();
